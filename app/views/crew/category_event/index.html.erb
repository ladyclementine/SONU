<div class="row">
   <div class="col-md-12">
      <div class="card-box">
         <div class="dropdown pull-right">
            <a href="#" class="dropdown-toggle card-drop" data-toggle="dropdown" aria-expanded="false">
            <i class="zmdi zmdi-more-vert"></i>
            </a>
            <ul class="dropdown-menu" role="menu">
               <li><a href="#">Action</a></li>
               <li><a href="#">Another action</a></li>
               <li><a href="#">Something else here</a></li>
               <li class="divider"></li>
               <li><a href="#">Separated link</a></li>
            </ul>
         </div>
         <h4 class="header-title m-t-0 m-b-30">Comitês</h4>
            <table  class="table m-0">
            <thead>
               <tr>
                  <th>Categoria</th>
                  <th>Comitê</th>
                  <th colspan="2">Configurações</th>
               </tr>
            </thead>
            <tbody>
              <% @category_event.each do |category_event| %>
                <tr>

                  <td><%= category_event.name %></td>
                  <td><%= category_event.comitee_id%></td>
                  <td><%= link_to 'Edit', edit_crew_category_event_path(category_event), class: 'btn btn-warning'  %></td>
                </tr>
              <% end %>
            </tbody>
          </table>

          <br>

          <%= link_to 'Nova categoria', new_crew_category_event_path, class: 'btn btn-primary' %>
      </div>
   </div>
   <!-- end col -->
</div>
